# Frontend env for local (non-Docker) development.
# Copy to .env.local and replace the placeholder — .env.local is gitignored.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=insert api key here
