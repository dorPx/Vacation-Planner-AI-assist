# Compose build args for the frontend image (Next.js inlines NEXT_PUBLIC_*
# at build time, so these must exist when `docker compose up --build` runs).
# Copy to .env and fill in — .env is gitignored, never commit real values.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
